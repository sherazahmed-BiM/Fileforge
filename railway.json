{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["apps/frontend/**"]
  },
  "deploy": {
    "startCommand": "cd apps/frontend && npm install && npm run build && npm run start",
    "healthcheckPath": "/"
  }
}
